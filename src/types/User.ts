export type User = {
  id: string;
  collectionId: string;
  collectionName: string;
  created: string;
  email: string;
  emailVisibility: boolean;
  name: string;
  updated: string;
  username: string;
  verified: boolean;
  avatar: string;
}